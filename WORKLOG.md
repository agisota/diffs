# DocDiffOps work log

## 2026-05-08 (Sprint 1, Day 1)

- `docker compose up --build -d` succeeded (~50 s build, BuildKit cache cold).
  Required `sudo`: the `dev` user is not in the `docker` group on this host;
  recorded so future automation can plan around it. Build log at
  `.work/day1-build.log`.
- `/health` returned `{"ok": true, "data_dir": "/data"}` and `/docs` rendered
  (200, 934 bytes); both captured under `.work/day1-artifacts/`.
- Smoke test ran end-to-end on the brief-mandated trio (1 PDF, 1 HTML, 1 XLSX):
  `POST /batches` → `bat_09d2c6c7e67d`, `POST /documents` accepted all three
  files (sha256 reported per doc), `POST /run?profile=fast&sync=true` finished
  in 2.46 s with `pairs=3, events=368, review_required=0`. `evidence_matrix.xlsx`
  downloaded clean (188 369 bytes); `executive_diff.md` is only 668 bytes — the
  brief §8.1 thinness audit was already true.
- Surprises that match the PLAN audit but bit during the smoke run: every doc
  came back with `source_rank=3` and `doc_type=null` (no `sources.yml` ingest);
  `review_required=0` despite 368 events because `classify_severity` never
  escalates without rank gating; the per-pair `track_changes.docx` was emitted
  for every pair regardless of source format (synthetic, not real DOCX redline).
- `docker compose down` clean. No volumes left dangling. Day-1 state is
  reproducible end-to-end on this host with a single `sudo docker compose up`.

## 2026-05-08 (Sprint 1, Day 2-3)

PR-1.1 — Postgres + Alembic baseline schema. Day 2 + Day 3 collapsed into a
single PR per task spec; the full Sprint-1 schema (7 tables) lands in one
consolidated migration rather than the two-step Day 2 / Day 3 split.

**Files added (10):**
- `docdiffops_mvp/alembic.ini` — alembic config at project root, points at
  `docdiffops/db/migrations`, reads URL from env via `env.py`.
- `docdiffops_mvp/docdiffops/db/__init__.py` — `engine`, `SessionLocal`,
  `get_session()` context manager. Honors `sqlite` `check_same_thread=False`.
- `docdiffops_mvp/docdiffops/db/models.py` — SQLAlchemy 2.x declarative
  models (`Base`, `Batch`, `Document`, `DocumentVersion`, `PairRun`,
  `DiffEvent`, `ReviewDecision`, `Artifact`) with FK ondelete, indexes on
  `(batch_id)` / `(pair_run_id)` / `(event_id)`, unique
  `(batch_id, sha256)` on `documents`, JSON columns for bbox, server-side
  `func.now()` timestamps with `onupdate` for `batches.updated_at`.
- `docdiffops_mvp/docdiffops/db/migrations/env.py` — patched to import
  `Base.metadata`, prepend the package root to `sys.path`, read
  `DATABASE_URL` from env (with the same `TESTING=1` fallback as
  `settings.py`), and use `render_as_batch` on SQLite for ALTER safety.
- `docdiffops_mvp/docdiffops/db/migrations/README` (+ `script.py.mako`,
  `versions/`) — created by `alembic init`.
- `docdiffops_mvp/docdiffops/db/migrations/versions/b852f3e2882b_baseline_schema.py`
  — single autogenerated migration; cleaned (removed boilerplate noise
  comments, renamed file). `upgrade()` and `downgrade()` are clean and
  drop in correct FK-safe reverse order.
- `docdiffops_mvp/requirements-dev.txt` — `pytest>=8`, `pytest-asyncio`.
- `docdiffops_mvp/tests/__init__.py`, `tests/unit/__init__.py` — package
  markers.
- `docdiffops_mvp/tests/unit/test_models.py` — 10 tests; SQLite default,
  Postgres opt-in via `TEST_DATABASE_URL`. Documented dialect discrepancy
  in the module docstring; fixture toggles `PRAGMA foreign_keys=ON` so the
  FK constraint test runs on either backend.

**Files modified (3):**
- `docdiffops_mvp/docker-compose.yml` — added `db` service (postgres:16-alpine,
  named `pgdata` volume, `pg_isready` healthcheck), exposed 5432 to host,
  changed `api`/`worker` `depends_on` to wait on `db: service_healthy`,
  injected `DATABASE_URL` into both.
- `docdiffops_mvp/requirements.txt` — added `sqlalchemy>=2.0,<2.1`,
  `alembic>=1.13,<2.0`, `psycopg2-binary>=2.9,<3.0`.
- `docdiffops_mvp/docdiffops/settings.py` — added `DATABASE_URL` (defaults
  to compose Postgres URL; SQLite memory under `TESTING=1`).

**Migration smoke test (against postgres:16-alpine over docker compose):**
- `alembic upgrade head` — 8 tables created (7 schema + `alembic_version`).
- `alembic downgrade base` — clean teardown, only `alembic_version` left.
- `alembic upgrade head` again — idempotent, single revision applied.
- `alembic current` — `b852f3e2882b (head)`.

**Tests passing: 10/10**
- 7 model round-trip tests (one per table)
- 3 constraint tests (`documents.sha256 NOT NULL`, `UNIQUE(batch_id, sha256)`,
  FK `diff_events.pair_run_id`)
- Suite runs in 0.22 s on SQLite, 0.87 s on Postgres.

**TODO / explicit non-scope (PR-1.2 and beyond):**
- `state.py`, `pipeline.py`, `main.py` untouched — dual-write lands in PR-1.2.
- No repository class yet; ORM models are pure schema.
- `EXTRACTOR_VERSION` / `COMPARATOR_VERSION` constants are referenced by the
  `document_versions` and `pair_runs` columns but not yet defined in
  `settings.py` — added in PR-1.6 alongside the cache key work.
- SQLite test fallback documented in test module docstring; production
  CI must pin `TEST_DATABASE_URL` to a Postgres URL.
